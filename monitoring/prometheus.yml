global:
  scrape_interval: 15s

scrape_configs:
  - job_name: ecall
    metrics_path: /metrics
    basic_auth:
      username: metrics
      password: change-me
    static_configs:
      - targets: ["app:4000"]
